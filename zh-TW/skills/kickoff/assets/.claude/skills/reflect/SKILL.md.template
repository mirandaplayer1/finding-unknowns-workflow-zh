---
name: reflect
description: 完工後的反思與記憶晉升。使用時機：一輪工作結束後在「新的對話」執行，或使用者說「反思」「復盤」「蒸餾教訓」時。請勿在工作對話中順手執行。
---

# 反思（Reflect）

目的：把工作層流水帳蒸餾成可長期使用的規則與教訓。必須在全新上下文的獨立對話執行——產出者自我評分會有確認偏誤。

## 步驟

1. 讀 `notes/implementation-notes.md`、`DECISIONS.md` 近期條目、`SPEC.md` 的規格變更記錄，以及 `/quiz` 產出的變更報告與答題結果（使用者答錯的地方是「決策記錄寫得不夠清楚」的訊號）。
2. **對照計畫與實際**：哪些偏離記錄反映的是規格缺陷（該補進 SPEC.md）、哪些是流程問題（候選為工作規則檔的規則）、哪些只是一次性意外（留在筆記即可）。
3. **晉升判定（門檻制）**：規則檔以 `WORKFLOW.md` 為準，不要寫進 CLAUDE.md——那只是 import 殼（舊專案若規則本來就寫在 CLAUDE.md 才沿用該檔）。
   - 同類教訓出現兩次以上 → 精簡成一條規則寫入規則檔的自我改進區，附一行來源。
   - 只出現一次 → 留在筆記層，本次不晉升。
   - 檢查規則檔既有規則：有沒有從未再被觸犯、已經內化的？提議刪除，維持規則檔精簡。
4. **決策記錄品質檢查**：抽查 DECISIONS.md 近期條目，「下次怎麼自己判斷」是否真的可帶走？空泛的改寫。
5. **收尾**：把 implementation-notes.md 歸檔到 `notes/archive/YYYY-MM-DD.md`，開一份乾淨的新筆記。
6. 向使用者報告：晉升了什麼、刪了什麼、為什麼，讓他可以否決。提案超過五條時做成 HTML 檢核頁（每條可勾「同意／否決」＋備註欄，底部「複製決定」按鈕），使用者貼回決定後才執行寫入。

## 原則

- 寧可少晉升：規則檔每一行都是每次對話的固定成本，過期規則比沒有規則更誤導。
- 所有寫入動作先列清單徵求同意，再執行。
- HTML 產出一律照 WORKFLOW.md〈HTML 產出與互動介面〉：內嵌留言層＋「複製結果」出口（現成片段 `notes/html-comment-layer.html`）。
